/**
 * Standardized Question Format Blueprint for AI Question Generation
 * 
 * Every question generated by the AI must strictly adhere to this schema:
 */
export const QUESTION_SCHEMA_BLUEPRINT = {
  // Unique slug identifier: e.g., 'js-beg-l1-q01' or 'html-med-l2-q03'
  id: "js-beg-l1-q01",
  
  // Technology stack: 'JavaScript' | 'HTML' | 'CSS'
  technology: "JavaScript",
  
  // Difficulty tier: 'Beginner' | 'Medium' | 'Advanced' | 'Expert'
  difficulty: "Beginner",
  
  // Level within tier (1 to 4)
  level: 1,
  
  // 1-indexed order within level (1 to 20)
  questionNumber: 1,
  
  // Question mode: 'BUILD' (code from scratch) | 'PREDICT_OUTPUT' (MCQ / trace) | 'DEBUG' (fix buggy code)
  type: "BUILD",
  
  // Concise title of challenge
  title: "Declare and Return a Greeting",
  
  // Detailed explanation of task and goals
  description: "Write a function `getGreeting()` that returns the exact string 'Hello, World!'.",
  
  // Core concepts exercised
  concepts: ["function", "return", "strings"],
  
  // Specific requirements to satisfy
  requirements: [
    "Define a function named `getGreeting`",
    "Return the exact string `'Hello, World!'`"
  ],
  
  // Constraints & edge conditions
  constraints: ["Return exact casing and punctuation"],
  
  // Input / Output test examples
  examples: [
    {
      input: "getGreeting()",
      output: "'Hello, World!'",
      explanation: "Direct invocation returns greeting string"
    }
  ],
  
  // Initial code shown in the Monaco code editor
  starterCode: `/**\n * Returns a simple greeting message.\n * @returns {string} 'Hello, World!'\n */\nfunction getGreeting() {\n  // Write your solution here\n  \n}\n`,
  
  // Reference solution for automated validation
  solutionCode: `function getGreeting() {\n  return 'Hello, World!';\n}`,
  
  // Only applicable when type === 'PREDICT_OUTPUT' (set to null for BUILD/DEBUG)
  predictConfig: null,
  
  // Multi-dimensional evaluation weight breakdown (totals 100)
  evaluationCriteria: {
    correctnessWeight: 70,
    qualityWeight: 15,
    structureWeight: 15,
    readabilityWeight: 0,
    bestPracticesWeight: 0
  },
  
  // Estimated completion time in minutes
  estimatedTime: 5,
  
  // Points awarded on successful submission
  points: 10
};

/**
 * Example Predict Output format for AI reference
 */
export const PREDICT_OUTPUT_SCHEMA_BLUEPRINT = {
  id: "js-med-l2-q02",
  technology: "JavaScript",
  difficulty: "Medium",
  level: 2,
  questionNumber: 2,
  type: "PREDICT_OUTPUT",
  title: "Event Loop & Microtask Execution Order",
  description: "Analyze the code snippet below and predict the exact console output order.",
  concepts: ["event loop", "microtasks", "macrotasks", "Promise"],
  requirements: ["Select the exact execution output sequence"],
  constraints: ["Standard browser runtime environment"],
  examples: [],
  starterCode: "",
  solutionCode: "",
  predictConfig: {
    snippet: `console.log('1');\nsetTimeout(() => console.log('2'), 0);\nPromise.resolve().then(() => console.log('3'));\nconsole.log('4');`,
    options: [
      { id: "opt-a", label: "1, 4, 3, 2", isCorrect: true },
      { id: "opt-b", label: "1, 2, 3, 4", isCorrect: false },
      { id: "opt-c", label: "1, 4, 2, 3", isCorrect: false },
      { id: "opt-d", label: "3, 1, 4, 2", isCorrect: false }
    ],
    correctAnswer: "1, 4, 3, 2",
    explanation: "Synchronous logs run first (1, 4), followed by the microtask queue (3), and macrotask timer (2)."
  },
  evaluationCriteria: {
    correctnessWeight: 100,
    qualityWeight: 0,
    structureWeight: 0,
    readabilityWeight: 0,
    bestPracticesWeight: 0
  },
  estimatedTime: 3,
  points: 15
};
